# DEXTRAH Multi-Object Grasp Prior Handoff

Timestamp: 2026-06-15T07:23:35Z

## Repository State

- Local worktree: `/home/lzha/code/.codex-worktrees/DEXTRAH/merge-dp-rgb-main-20260613`
- Branch: `main`
- Current local/source commit before this handoff commit: `48286985bb7adf2d0c8faa61d2689d330c31d407`
- Remote l401 source worktree: `/lustre/fsw/portfolios/nvr/users/lzha/src/worktrees/DEXTRAH/multiobject-topdown-axis-20260615-753139c`
- Remote l401 source HEAD: `48286985bb7adf2d0c8faa61d2689d330c31d407`
- Main worklog: `worklogs/dextrah-multiobject-grasp-prior/dextrah-multiobject-grasp-prior-20260613T003321Z.md`

Recent relevant commits:

- `4828698` Track verified grasp pass rates
- `2e54ddf` Require robust verified grasp observations
- `6de384a` Prefer explicit verified grasp indices
- `f9de3a4` Allow custom multiobject validation camera views
- `1b31a8b` Reject shallow multiobject grasp prior approaches

## User Concern Being Addressed

The selected grasp pose must never approach from below the object. Bottom or shallow underside approaches collide with the table and create unrealistic or buggy resets.

Current implementation status:

- Multi-object grasp reset now gates candidate pregrasp direction in world coordinates with `grasp_prior_reset_min_pregrasp_z`, defaulted to `0.70`.
- The same top-side/table invariants are checked after IK through `_grasp_prior_reset_topdown_mask`.
- Table clearance is checked against both pregrasp EE z and contact-reference z.
- Video metrics report selected pregrasp direction and candidate topdown/table counts.
- Verified-grasp caches now prefer top-level `indices_by_uuid` over stale `objects` metadata.

## Key Code Changes

Files changed in recent work:

- `dextrah_lab/tasks/dextrah_franka_multi_object_grasp/franka_multi_object_grasp_env.py`
  - Rejects shallow/bottom approaches by world-frame pregrasp direction.
  - Adds table-valid reset/quality gating.
  - Fixes verified cache loading to prefer `indices_by_uuid`.
- `dextrah_lab/rl_games/collect_franka_multi_object_verified_grasps.py`
  - Exports top-level `indices_by_uuid`.
  - Tracks `attempt_observations`, `pass_observations`, and `pass_rate` per grasp index.
  - Supports `--min_pass_observations_per_index` and `--min_pass_rate_per_index`.
- `cluster/sbatch_collect_franka_multi_object_verified_grasps_1gpu.sh`
  - Passes robust cache thresholds through Slurm environment.
- `dextrah_lab/rl_games/validate_franka_multi_object_grasp_videos.py`
  - Supports custom validation camera.
  - Emits selected approach vector diagnostics.
- `cluster/sbatch_validate_franka_multi_object_grasp_videos_1gpu.sh`
  - Passes custom camera settings.

Validation checks already run:

- `python3 -m py_compile dextrah_lab/tasks/dextrah_franka_multi_object_grasp/franka_multi_object_grasp_env.py dextrah_lab/rl_games/validate_franka_multi_object_grasp_videos.py`
- `python3 -m py_compile dextrah_lab/rl_games/collect_franka_multi_object_verified_grasps.py`
- `bash -n cluster/sbatch_collect_franka_multi_object_verified_grasps_1gpu.sh`
- `bash -n cluster/sbatch_validate_franka_multi_object_grasp_videos_1gpu.sh`
- `git diff --check`

## Data And Cache Paths

Object manifests:

- Two-object manifest: `/results/assets/filtered_manifests/train2_7195_b87_nobelow_d053e6c_20260615T0045Z/manifest.json`
- Object0 manifest: `/results/assets/filtered_manifests/train2_7195_b87_nobelow_d053e6c_20260615T0045Z/manifest_object0_7195ed3346a445448308febe833c180a.json`
- Object1 manifest: `/results/assets/filtered_manifests/train2_7195_b87_nobelow_d053e6c_20260615T0045Z/manifest_object1_b87a65917e494aa4b306aeb6ee961182.json`

Stable pose cache:

- `/results/validations/train2_7195_b87_nobelow_d053e6c_20260615T0045Z/settled_pose_cache`

Important verified grasp caches:

- Initial top-side cache: `/results/assets/verified_grasp_indices/verified_topside70_min1_train2_7195_b87_1b31a8b_20260615T0554Z/verified_indices.json`
- Diagnostic forced-index cache for object1 index `929`: `/results/assets/verified_grasp_indices/verified_topside70_obj1_only929_f9de3a4_20260615T0624Z/verified_indices.json`
- Strict pass-rate collection output: `/results/assets/verified_grasp_indices/verified_robust_obs3_rate50_train2_7195_b87_4828698_20260615T0706Z/verified_indices.json`
- Derived practical robust cache: `/results/assets/verified_grasp_indices/verified_robust_obs3_rate10_train2_7195_b87_4828698_20260615T0712Z/verified_indices.json`

The derived practical cache exports:

- object0 `7195ed3346a445448308febe833c180a`: index `354`
- object1 `b87a65917e494aa4b306aeb6ee961182`: index `1075`

## Validation Evidence

Passing/default-cache evidence:

- Job `1029599`, object0, cache `1b31a8b`: reset/perturb/grasp passed; selected index `354`; approach z `0.7485`; finger-table clearance min `0.0510m`.
- Job `1029600`, object1, cache `1b31a8b`: reset/perturb/grasp passed; selected index `1075`; approach z `0.9987`; finger-table clearance min `0.0944m`.

Close-camera diagnostic evidence:

- Job `1029661`, object1 forced index `929`, source `6de384a`:
  - reset and perturbation passed.
  - selected sample index `929`.
  - selected approach z `0.8658`.
  - `128/128` candidates passed topdown/table gates.
  - finger-table clearance min `0.1037m`.
  - grasp failed: lift `0.0207m`, best probe lift `0.0702m`.
  - Interpretation: no bottom/table collision; index `929` is contact-poor under the randomized pose.
  - Local video: `cluster_results/l401/franka_multi_grasp_video_topside70_obj1_b87a_idx929_closecam_6de384a_20260615T0649Z/grasp_contact.mp4`
  - Viewer URL used: `http://localhost:8765/view?path=cluster_results/l401/franka_multi_grasp_video_topside70_obj1_b87a_idx929_closecam_6de384a_20260615T0649Z/grasp_contact.mp4`

Robust cache collection:

- Job `1029665`, strict `MIN_PASS_RATE_PER_INDEX=0.50`, canceled after cycle 29 because it exported no indices.
- Useful stats at cancellation:
  - object0 index `354`: `112/917` passes, pass rate `0.122`, lift `0.299`, approach z `0.748`.
  - object1 index `1075`: `54/173` passes, pass rate `0.312`, lift `0.163`, approach z `0.999`.
- Based on these stats, derived rate-0.10 cache was created.

Canceled validation:

- Job `1029670`, object1 robust cache, `NUM_ENVS=16`, `GRASP_RESET_ATTEMPTS=24`, canceled after `00:14:08` because it stayed in pre-capture scoring with zero grasp frames.
- Job `1029677`, bounded object1 robust cache, `NUM_ENVS=8`, `GRASP_RESET_ATTEMPTS=6`, canceled at user request after `00:02:32`; it had `reset_settle=108`, `perturbation=48`, `grasp_contact=0`, no metrics.

## Active Jobs

Stopped on 2026-06-15T07:23Z:

- `1029677` `dextrah_franka_multi_vid`: canceled.
- `1029676` `dextrah_cube_rgb_dp_train`: canceled.

Final l401 queue check after cancel showed no active l401 jobs for `lzha`.

An unrelated A100 CPU job was visible and intentionally not canceled:

- `29061545` `molmo2_yam_tfds` on `a1001`, partition `cpu_long`.

## Current Diagnosis

The bottom-approach issue is addressed at the reset-selection level:

- The selected reset candidates in diagnostics are top-side by world-frame approach vector.
- The table clearance metrics are positive.
- Close-camera video shows no underside/table collision for index `929`.

The remaining blocker before RL training is grasp robustness:

- Some top-side GraspGen prior indices work only for a subset of randomized object poses/yaws.
- Index `929` is top-side but contact-poor and should not be used for training resets.
- Index `1075` is the best current object1 index, but its observed pass rate is only about `0.31` in the strict collector statistics.
- Validation scoring with many envs/attempts is currently too slow because it performs expensive warmstart rollouts before recording.

## Recommended Next Steps

1. Keep the no-bottom invariant as hard policy. Do not relax `GRASP_RESET_MIN_PREGRASP_Z=0.70`.
2. Improve validation scorer runtime:
   - Add a max scoring attempt/time option or early-stop once a candidate reaches pass threshold.
   - Prefer scoring fewer warmstart steps or score reset geometry before running full lift rollout.
3. Validate object1 index `1075` from the derived robust cache with a bounded scorer that actually writes frames/metrics.
4. If object1 remains unreliable, regenerate verified grasps with stricter contact-quality gates, not just top-side gates:
   - Require multiple pass observations.
   - Use a practical pass-rate threshold around `0.10` to `0.25` for this small debug set.
   - Exclude index `929`.
5. Only after reset videos are visually clean for object0 and object1 should the next agent continue RL training.

## Notes For Fresh Agent

- Always use `/lustre/fsw/portfolios/nvr/users/lzha/...` for cluster data/artifacts, not `/home`.
- The l401 remote cannot reliably fetch GitHub by SSH; deploy source changes with Git bundle into the remote worktree.
- Use `CODE_NFS=/lustre/fsw/portfolios/nvr/users/lzha/src/worktrees/DEXTRAH/multiobject-topdown-axis-20260615-753139c` in Slurm exports. If omitted, wrappers may fall back to canonical `/lustre/.../src/DEXTRAH`, which was stale and missing validation scripts.
- Use `viz-open` for local videos.
- Do not treat Slurm success/failure as enough; inspect `video_metrics.json` and videos.
